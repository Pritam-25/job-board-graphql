scalar DateTime

type User {
  id: String!
  email: String!
  name: String
  jobsOwned: [Job!]!
  appliedJobs: [Job!]!
  createdAt: DateTime!
  updatedAt: DateTime!
}

extend type Query {
  users: [User!]
}
